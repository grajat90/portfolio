import { ArrowUpRight, Download, ArrowRight } from "lucide-react";

export function IconArrowUpRight({ className }: { className?: string }) {
  return <ArrowUpRight className={className} strokeWidth={2} />;
}

export function IconDownload({ className }: { className?: string }) {
  return <Download className={className} strokeWidth={2} />;
}

export function IconArrowRight({ className }: { className?: string }) {
  return <ArrowRight className={className} strokeWidth={2} />;
}
